<%= render 'shared/errors', obj: @category %>
      
<%= form_with model: @category, class:"shadow p-3 mb-3 bg-info bg-body-tertiary rounded", local: true do |f| %>
  <div class="form-group row mb-4">
    <%= f.label :name, "Category name", class: "col-2 col-form-label text-light" %><br/>
    <div class="col-10">
      <%= f.text_field :name, class: "form-control shadow-rounded", placeholder: "Enter a name" %>
    </div>
  </div>

  <div class="form-group row col-4 justify-content-center">
    <%= f.submit class: "btn btn-outline-light btn-lg" %>
  </div>
<% end %>